
import React from "react";
import { AppShell } from "@/components/dashboard/AppShell";
import { AIHistoryContent } from "@/components/ai/AIHistoryContent";

const AIHistory = () => {
  return (
    <AppShell>
      <AIHistoryContent />
    </AppShell>
  );
};

export default AIHistory;
