
import React from "react";
import { AppShell } from "@/components/dashboard/AppShell";
import { AIStrategyContent } from "@/components/ai/AIStrategyContent";

const AIStrategy = () => {
  return (
    <AppShell>
      <AIStrategyContent />
    </AppShell>
  );
};

export default AIStrategy;
